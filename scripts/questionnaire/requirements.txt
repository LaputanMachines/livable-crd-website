# Needed by everything in this directory, all of which writes to the sheet and
# so needs an authenticated client. The rest of scripts/ only reads, and is
# stdlib-only.
gspread>=6.2
google-auth>=2.0
